{
  "id": "{{elasticsearch.framework-name}}",
  "cpus": 1,
  "mem": {{elasticsearch.scheduler.ram}},
  "ports": [0],
  "instances": {{elasticsearch.scheduler.instances}},
  "healthChecks": [
    {
      "gracePeriodSeconds": 120,
      "intervalSeconds": 30,
      "maxConsecutiveFailures": 0,
      "path": "/",
      "portIndex": 0,
      "protocol": "HTTP",
      "timeoutSeconds": 5
    }
  ],
  "args": ["--zookeeperMesosUrl", "{{mesos.master}}", "--elasticsearchCpu", "1.0", "--elasticsearchDisk", "200", "--elasticsearchNodes", "1", "--elasticsearchRam", "512"],
  "env": {
    "JAVA_OPTS": "{{elasticsearch.scheduler.java-heap}}"
  },
  "container": {
    "type": "DOCKER",
    "docker": {
      "image": "mesos/elasticsearch-scheduler",
      "network": "HOST",
      "forcePullImage": {{elasticsearch.forcePullImage}}
    }
  },
  "ports": [{{elasticsearch.port}}],
  "requirePorts": true
}
